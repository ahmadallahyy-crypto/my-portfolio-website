# TODO - Mute Vite warnings

- [ ] Implement warning-suppression in `my-portfolio/vite.config.js`.
- [ ] Run `npm run dev` / `npm run build` to verify warnings are muted.
- [ ] If too much is hidden, refine filter to only suppress specific warning prefixes.
